<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/loading_screen_id"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/alpha_white_90"
    android:clickable="true">

    <ImageView
        android:id="@+id/loading_animation_id"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/loading_screen_text_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/loading_animation_id"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="64dp"
        android:layout_marginTop="64dp"
        android:textColor="@color/grey_5"
        android:layout_marginEnd="64dp"
        android:fontFamily="@font/manrope_medium"
        android:gravity="center"
        android:textSize="16sp"
        tools:text="This might take longer depending on your internet connection and the number of pictures added" />
</RelativeLayout>